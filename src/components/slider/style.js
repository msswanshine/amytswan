import { css } from 'emotion';

const sliderWrapper = css`
  position: relative;
  width: calc(100vw - 18%);
  margin: 100px auto;
  overflow: hidden;
  padding: 40px 0;
`;

export default sliderWrapper;
